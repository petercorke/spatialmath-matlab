language: java
install: source /dev/stdin <<< "$(curl -sL https://ssd.mathworks.com/supportfiles/matlab-runner/install)"
script: matlab-runner "cd unit_test; addpath ..; results = runtests('IncludeSubfolders',false); assert(all(~[results.Failed]))"
